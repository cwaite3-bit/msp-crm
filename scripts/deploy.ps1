# This script has moved to C:\Scripts\MSP\deploy.ps1 (with a
# C:\Scripts\MSP\deploy.bat launcher next to it for double-click use).
#
# It now lives outside the msp-crm repo entirely - same pattern as your
# other apps' deploy scripts (C:\Scripts\Peptides, C:\Scripts\TJ) - so it
# points at this repo folder rather than being part of it.
#
# This file is left here only as a pointer and can be deleted; see
# deployment-runbook.md (in the "Quoting and Customer Tracking tool"
# Claude project) for the current instructions.

Write-Host "This script has moved to C:\Scripts\MSP\deploy.ps1 (or double-click C:\Scripts\MSP\deploy.bat)." -ForegroundColor Yellow
Read-Host "Press Enter to close"
